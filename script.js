//Global Variables
//let taskCount = 1;
//let tasks = [];

//Task Class

	//TaskObject.id = `Task ${taskCount}`;
	//TaskObject.description = value from user-input form
	//TaskObject.isComplete = false;

function update() {
	//for item in task list:
		//add task
}

function addTask() {
	//instantiate class object to `task${taskCount}`, taskCount++
	//add object to tasks list
	//update html ul class='list'
}

function deleteTask() {
	//delete object from tasks list
	//update html ul class='list'
}

function completeTask() {
	//set taskObject.isComplete to true
	//update html ul class='list'
}